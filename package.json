{
    "name": "papercast",
    "version": "1.0.0",
    "scripts": {
        "start": "node backend/index.js",
        "build": "cd frontend && npm install && npm run build"
    },
    "engines": {
        "node": ">=20.19.0"
    }
}